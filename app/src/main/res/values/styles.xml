<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Dialog start -->
    <style name="materialDialogSheet" parent="@android:style/Theme.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <!-- TextView start -->
    <style name="TextViewStyle_50px_ffffff">
        <item name="android:textSize">@dimen/글씨_50PX</item>
        <item name="android:textColor">@color/color_ffffff</item>
    </style>

    <style name="TextViewStyle_60px_ffffff">
        <item name="android:textSize">@dimen/글씨_60PX</item>
        <item name="android:textColor">@color/color_ffffff</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="dialogBtnLeft">
        <item name="android:textSize">18dp</item>
        <item name="android:textColor">@drawable/selector_txt_white</item>
        <item name="android:background">@drawable/selector_btn_choice_left</item>
    </style>

    <style name="dialogBtnMiddle">
        <item name="android:textSize">18dp</item>
        <item name="android:textColor">@drawable/selector_txt_white</item>
        <item name="android:background">@drawable/selector_btn_choice_middle</item>
    </style>

    <style name="dialogBtnRight">
        <item name="android:textSize">18dp</item>
        <item name="android:textColor">@drawable/selector_txt_white</item>
        <item name="android:background">@drawable/selector_btn_choice_right</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
